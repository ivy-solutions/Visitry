{
  // Server authentication info
  "servers": [
    {
      "host": "dev.visitry.org",
      "username": "ubuntu",
      //"password": "password",
      // or pem file (ssh based authentication)
      // WARNING: Keys protected by a passphrase are not supported
      "pem": "~/.ssh/id_staging-server",
      // Also, for non-standard ssh port use this
      //"sshOptions": { "port" : 49154 },
      // server specific environment variables
      "ssl": {
        "certificate": "/etc/letsencrypt/live/dev.visitry.org/fullchain.pem", // this is a bundle of certificates
        "key": "/etc/letsencrypt/live/dev.visitry.org/privkey.pem", // this is the private key of the certificate
        "port": 443 // 443 is the default value and it's the standard HTTPS port
      },
      "env": {}
    }
  ],

  // Install MongoDB on the server. Does not destroy the local MongoDB on future setups
  "setupMongo": true,

  // WARNING: Node.js is required! Only skip if you already have Node.js installed on server.
  "setupNode": true,

  // Application name (no spaces).
  "appName": "Visitry",

  // Location of app (local directory). This can reference '~' as the users home directory.
  // i.e., "app": "~/Meteor/my-app",
  // This is the same as the line below.
  "app": "../../../../Visitry",

  // Configure environment
  // ROOT_URL must be set to your correct domain (https or http)
  // will be: http://visitry.org/visitry 
  "env": {
    "PORT": 80,
    "ROOT_URL": "https://dev.visitry.org"
  },

  // Meteor Up checks if the app comes online just after the deployment.
  // Before mup checks that, it will wait for the number of seconds configured below.
  "deployCheckWaitTime": 30,

  // show a progress bar while uploading. 
  // Make it false when you deploy using a CI box.
  "enableUploadProgressBar": true,

  "buildOptions": {
    // build with the debug mode on 
    "debug": true
  }
}
